In order to speed up processes using WSL instead of Windows (let Ubuntu do the work faster):

1. To copy files from Windows to WSL:
cp -r /mnt/c/Users.../Directory_to_copy ~/Directory_to_paste

2. To later update changes back into Windows:
rsync -av --progress ~/Directory_to_paste/ /mnt/c/Users.../Directory_to_copy
